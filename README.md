# pdf-summariser-chatbot-
AI From Mars is a document-based conversational chatbot built using a Retrieval-Augmented Generation (RAG) architecture. The system allows users to upload a PDF document and interact with it through a chat interface. Instead of relying on the model’s general knowledge, the chatbot retrieves relevant information from the uploaded document and ge
